I"–<h2 id="pontifications">Pontifications</h2>

<div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>find <span class="nb">.</span> <span class="nt">-type</span> f <span class="nt">-exec</span> <span class="nb">echo</span> <span class="o">{}</span> <span class="se">\;</span> | <span class="nb">grep</span> <span class="s2">"custom.node-browser"</span>
</code></pre></div></div>

<p>searches for the string <code class="highlighter-rouge">custom.node-browser</code> in files (NOT directories) (not sure why we need the whole <code class="highlighter-rouge">-exec echo {} \;</code> dance (is this an OS X / BSD thing?)!</p>
:ET