I"/<h2 id="pontifications">Pontifications</h2>

<ul>
  <li>First I had to install ruby:</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew upgrade rbenv ruby-build
rbenv <span class="nb">install </span>2.5.0
rbenv global 2.5.0
vi ~/.bash_profile <span class="c"># and add eval "$(rbenv init -)"</span>
</code></pre></div></div>

<ul>
  <li>Then jekyll and surge.sh</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gem <span class="nb">install </span>jekyll <span class="c"># my syntax colouring needs red carpet</span>
gem <span class="nb">install </span>redcarpet
<span class="nb">sudo </span>npm <span class="nb">install</span> <span class="nt">--global</span> surge
</code></pre></div></div>
:ET