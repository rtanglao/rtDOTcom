I"x<h1 id="pontifications">Pontifications</h1>

<ul>
  <li>
    <p>Use <code class="highlighter-rouge">--renderToDisk</code> or there will be nothing rendered. i.e. no HTML web pages just cached version in RAM.  It doesn’t seem be much slower then rendering to RAM!</p>
  </li>
  <li>
    <p>The full command line to run the local server is:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hugo server <span class="nt">--buildDrafts</span> <span class="nt">--disableFastRender</span> <span class="nt">--renderToDisk</span> Y&amp;
</code></pre></div>    </div>
  </li>
</ul>

:ET