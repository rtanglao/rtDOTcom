I"Æ<h2 id="pontifications">Pontifications</h2>

<ul>
  <li>have to re-compile ruby from scratch otherwise jekyll gem wonâ€™t build, no idea why :-)</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update <span class="nt">-y</span> <span class="o">&amp;&amp;</span> <span class="nb">sudo </span>apt-get upgrade <span class="nt">-y</span>
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> build-essential ruby-full
<span class="nb">sudo </span>gem update <span class="nt">--system</span>
<span class="nb">sudo </span>gem <span class="nb">install </span>jekyll bundler
<span class="nb">sudo </span>gem <span class="nb">install </span>github-pages
apt-get <span class="nb">install </span>zlib1g-dev
<span class="nb">sudo </span>apt-get <span class="nb">install </span>zlib1g-dev
<span class="nb">sudo </span>gem <span class="nb">install </span>github-pages <span class="c"># don't need this</span>
<span class="nb">sudo </span>gem <span class="nb">install </span>redcarpet
curl <span class="nt">-sL</span> https://deb.nodesource.com/setup_8.x | <span class="nb">sudo</span> <span class="nt">-E</span> bash -
<span class="nb">sudo </span>apt-get <span class="nb">install</span> <span class="nt">-y</span> nodejs 
<span class="nb">sudo </span>npm <span class="nb">install</span> <span class="nt">--global</span> surge
</code></pre></div></div>
:ET